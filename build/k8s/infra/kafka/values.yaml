provisioning:
  enabled: true
  topics:
    - name: users-events
      replicationFactor: 1
    - name: create-order-saga-commands
      replicationFactor: 1

listeners:
  client:
    protocol: 'PLAINTEXT'
  controller:
    protocol: 'PLAINTEXT'
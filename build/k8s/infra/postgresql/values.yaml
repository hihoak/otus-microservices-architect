primary:
  resourcesPreset: nano

readReplicas:
  resourcesPreset: nano

metrics:
  enabled: true
  resourcesPreset: nano
  service:
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/path: "/metrics"
      prometheus.io/port: "9187"
